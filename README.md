# Description
Fast API backend for evaluating business opportunities in real time

## Tech Stack
Fast API, Pydantic
SMTP, SSl, MIMEText

## Installation
pip install -r requirements.txt

## How to run it
Make sure you are in app sub directory. Then run the following command:
uvicorn main:app --reload